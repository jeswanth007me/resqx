import type { SimulationState } from '../types/simulation';
import type { Telemetry } from '../types/telemetry';
export const buildTelemetry = (state: SimulationState): Telemetry => ({ timestamp: state.simulationTime, ambulance: { id: state.ambulance.id, position: state.ambulance.position, speed: state.ambulance.speed, destination: state.ambulance.destination, eta: Math.max(0, Math.round((1 - state.ambulance.progress) * 52)), emergencyStatus: state.ambulance.status }, route: { currentRoad: state.roads[Math.min(2, Math.floor(state.ambulance.progress * 3))]?.id ?? 'ROAD-03', nextSignal: state.ambulance.currentSignal, remainingDistance: Math.max(0, Math.round((1 - state.ambulance.progress) * 760)) }, signals: state.signals.map((signal) => ({ id: signal.id, state: signal.state, distanceFromAmbulance: Math.round(Math.hypot(signal.position.x - state.ambulance.position.x, signal.position.y - state.ambulance.position.y)), queueLength: signal.queueLength })), traffic: { vehicleCount: state.vehicles.length, congestionLevel: state.vehicles.length > 6 ? 'HIGH' : state.vehicles.length > 3 ? 'MODERATE' : 'LOW' } });